@import 'sass/_true.styl'

// +describe('is-px()0') {
  // +describe('is-px()1') {
  //   +it('fifth it') {
  //     +assert() {
  //       +output() {
  //         width: 14em + 2;
  //       }
  //       +expect() {
  //         width: 16em;
  //       }
  //     }
  //   }
  // }
  +describe('is-px()2') {
    +it('fifth it') {
      assert-true(true)
      assert-true(false)
      assert-true(true)
      assert-true(true)
    }

    +it('sixth it') {
      assert-true(true)
      assert-true(false)
      assert-true(true)
      assert-true(true)
    }
  }

  +describe('is-px()3') {
    +it('seventh it') {
      assert-true(true)
      assert-true(true)
      assert-true(true)
      assert-true(true)
    }

    +it('eight it') {
      assert-true(true)
      assert-true(true)
      assert-true(true)
      assert-true(true)
    }
  }
// }

report()