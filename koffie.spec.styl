@import 'sass/_true.styl'

+describe('is-px()1')
  +it('first it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
  +it('second it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
+describe('is-px()2')
  +it('third it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
  +it('fourth it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
+describe('is-px()3')
  +it('fifth it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
  +it('sixth it')
    assert-true(true)
    assert-true(false)
    assert-true(true)
    assert-true(true)
  +describe('is-px()4')
    +it('seventh it')
      assert-true(true)
      assert-true(false)
      assert-true(true)
      assert-true(true)
    +it('eight it')
      assert-true(true)
      assert-true(false)
      assert-true(true)
      assert-true(true)


